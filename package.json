{
  "name": "polarity-extractor",
  "version": "0.1.0",
  "private": true,
  "description": "Web app that extracts fiber-optic polarity maps from engineering drawings using Claude vision and injects them into test-plan templates.",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r exec rm -rf dist node_modules .turbo && rm -rf node_modules"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.9"
}
